test_batch_size: 128  
num_workers: 4
budgets: [0.8, 0.92, 0.94, 0.96, 0.98, 1.0]         # in case where the model has a budget, this is the budget to use. Leave null otherwise
noises:  []                         # in case where the model has a noise, this is the noise to use. Leave null otherwise. Notice that this only has effect ig noise != null in test_config 
num_images: 4
hard_mask: True

cumulative_plot: True
cumulative_plot_dir: ${experiments_dir}/${experiment_name}
run_names: null